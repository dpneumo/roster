  <% info = mdl.element_info[elem] %>
  <% disable = editing && info[:disable_edit] %>
  <div class="mx-4 mb-5 min-w-0">
    <label  for=<%= info[:lblfor] %>
            class="form-field-label h-full pr-2">
      <%= info[:lbltxt] %>
      <%= mdl.form.check_box( elem, 
            class: "block w-full mt-4 text-gray-900 font-medium rounded-md border-0 shadow ring placeholder:text-gray-400" ) %>
    </label>
  </div>