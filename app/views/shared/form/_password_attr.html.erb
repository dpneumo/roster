  <% info = mdl.element_info[elem] %>
  <div class="mx-4 mb-5 min-w-0">
    <label  for=<%= info[:lblfor] %>
            class="form-field-label">
      <%= info[:lbltxt] %>
      <%= mdl.form.password_field( elem, id: info[:lblfor], class: "base-field ring" ) %>
    </label>
  </div>