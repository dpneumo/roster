<div class="flex flex-col max-w-full mx-auto">
  <div class="inline-block min-w-full py-2 overflow-hidden">
     
    <table class="min-w-full text-left text-sm font-medium bg-blue-600">
      <thead class="bg-gray-300 font-medium dark:border-neutral-800 dark:bg-neutral-300">
        <%= render head %>
      </thead>
      <tbody>
        <% collection.each_with_index do |item, i| %>
          <% background = i.even? ? 'bg-white' : 'bg-gray-200' %>
          <%= render item_row, item: item, background: background %>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
