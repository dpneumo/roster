<div class="max-w-4xl mb-5">
  <h1 class="text-2xl font-extrabold">New Ownership</h1>

  <%= render 'shared/form', mdl: @ownership %>

  <hr class="border-1 border-gray-700" />

  <%= link_to 'Ownerships List', ownerships_path, class: "text-lg link-hover" %>
</div>