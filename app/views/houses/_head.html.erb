<tr>
	<th scope="col" class="th-pad">
	  <%= sort_link(@q,:house_address, [:street, :number], 'Address', class: 'font-sans') %>
	</th>

	<th scope="col" class="th-pad hidden md:table-cell">
	  <%= sort_link(@q,:flag, class: 'font-sans') %>
	</th>

	<th scope="col" class="th-pad hidden lg:table-cell">
	  <%= sort_link(@q,:rental, class: 'font-sans') %>
	</th>

	<th scope="col" class="th-pad hidden lg:table-cell">
	  <%= sort_link(@q,:listed, class: 'font-sans') %>
	</th>

	<th scope="col" class="th-pad hidden lg:table-cell">
	  <%= sort_link(@q,:status, class: 'font-sans') %>
	</th>

	<th scope="col" class="th-pad font-sans hidden xl:table-cell">Linked Lots</th>
	<th scope="col" class="th-pad font-sans hidden md:table-cell">Notes</th>
</tr>
